package com.ordolabs.chessmate.di

import org.koin.dsl.module

val useCaseModule = module {

}

// TODO: [check] delete if unused
internal enum class UseCase(val label: String) {
    // nothing's here for a while
}